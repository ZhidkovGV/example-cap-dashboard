import { ConverToPercentsPipe } from './conver-to-percents.pipe';

describe('ConverToPercentsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverToPercentsPipe();
    expect(pipe).toBeTruthy();
  });
});
